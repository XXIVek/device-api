openapi: 3.0.0
info:
  title: Device API
  description: API for exchanging messages and files between devices
  version: 1.0.0
servers:
  - url: http://localhost/device_api/public
    description: Local server
paths:
  /api/v1/messages:
    post:
      summary: Send a message to another device
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                recipient_id:
                  type: integer
                  description: ID of the recipient device
                subject:
                  type: string
                message:
                  type: string
                  description: JSON string with the message content
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Message created
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message_id:
                    type: string
                    format: uuid
    get:
      summary: Get pending messages for the current device
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of messages
          content:
            application/json:
              schema:
                type: array
              items:
                $ref: '#/components/schemas/Message'
  /api/messages/{id}:
    get:
      summary: Get a specific message metadata
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Message details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageDetail'
    delete:
      summary: Delete a message (confirm delivery)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Message deleted
  /api/messages/{id}/file:
    get:
      summary: Download attached file
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Token
  schemas:
    Message:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: UUID сообщения (BINARY(16) в БД)
        sender_uuid:
          type: string
          format: uuid
          description: UUID устройства-отправителя (CHAR(36) в БД)
        recipient_uuid:
          type: string
          format: uuid
          description: UUID устройства-получателя (CHAR(36) в БД)
        subject:
          type: string
          description: Тема сообщения (VARCHAR(255) в БД)
        body:
          type: string
          format: byte
          description: Тело сообщения — JSON-данные (LONGTEXT в БД)
        file_path:
          type: string
          nullable: true
          description: Путь к прикреплённому файлу (VARCHAR(512) в БД)
        status:
          type: string
          enum: [pending, delivered]
          description: Статус сообщения (ENUM в БД: pending, delivered и др.)
        created_at:
          type: string
          format: date-time
          description: Время создания сообщения (TIMESTAMP в БД)
        delivered_at:
          type: string
          format: date-time
          nullable: true
          description: Время доставки сообщения (TIMESTAMP в БД)
    MessageDetail:
      allOf:
        - $ref: '#/components/schemas/Message'
        - type: object
          properties:
            metadata:
              type: object
              description: Декодированные метаданные из body (JSON)

